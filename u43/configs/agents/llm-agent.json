{
  "id": "llm_agent",
  "name": "LLM Agent",
  "description": "Chat with or make decisions using Large Language Models",
  "version": "1.0.0",
  "category": "ai",
  "icon": "message-circle",
  "inputs": {
    "message": {
      "type": "string",
      "required": true,
      "label": "Message",
      "description": "The message to send to the LLM"
    },
    "conversation_history": {
      "type": "array",
      "required": false,
      "label": "Conversation History",
      "description": "Previous messages in the conversation (array of objects with 'role' and 'content')"
    },
    "system_prompt": {
      "type": "string",
      "required": false,
      "label": "System Prompt",
      "description": "Optional system prompt to set the assistant's behavior"
    },
    "context": {
      "type": "object",
      "required": false,
      "label": "Context",
      "description": "Additional context for decision-making (used in decision mode)"
    }
  },
  "outputs": {
    "response": {
      "type": "string",
      "label": "Response"
    },
    "reason": {
      "type": "string",
      "label": "Reason",
      "description": "Reasoning for the decision (only in decision mode)"
    }
  },
  "handler": "U43\\Agents\\Built_In\\LLM_Agent\\LLM_Agent",
  "settings": {
    "provider": "openai",
    "model": "gpt-5.2",
    "temperature": 0.7,
    "system_prompt": "You are a helpful assistant.",
    "mode": "chat"
  }
}

